# this install the build essentials for openseadragon on a ubuntu 13.04 system

sudo apt-get install npm

sudo apt-get purge nodejs

sudo rm /usr/local/bin/grunt

sudo rm /usr/local/bin/grunt-less

need newest version of nodejs sudo add-apt-repository ppa:chris-lea/node.js

sudo apt-get update

sudo apt-get install nodejs

sudo npm install grunt-cli -g

sudo rm -r node_modules

sudo npm install grunt

sudo npm install grunt-contrib-uglify

sudo npm install grunt-recess

sudo ln -s /usr/bin/nodejs /usr/bin/node

sudo npm install -g grunt-cli

sudo npm install grunt-contrib-compress grunt-contrib-concat grunt-contrib-jshint grunt-contrib-qunit grunt-contrib-connect grunt-contrib-watch grunt-contrib-clean grunt-git-describe grunt-text-replace

sudo ln -s /usr/bin/grunt /usr/local/bin/grunt

